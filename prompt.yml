# system:
- EPANET 3
- CMake 3.22

# sources:
paper: no
  main.md: all

networks: no
  any-town.inp: all
  any-town.md: none

codes: no
  bbsolver.py: all  
  economic_custom.py: all

epanet-dev/src/CLI: yes
  BBTests.h: all
  BBCounter.h: none
  BBCounter.cpp: none
  BBStats.h: none
  BBStats.cpp: none
  BBSolver.h: all
  BBSolver.cpp: all
  BBConstraints.h: none
  BBConstraints.cpp: none
  Helper.h: none
  Helper.cpp: none
  Console.h: none
  Console.cpp: none
  Utils.h: none
  Utils.cpp: none
  Profiler.h: none
  main.cpp: none

epanet-dev/src/Elements: no
  pump.h: all

epanet-dev/: no
  makefile: all

.venv/lib/python3.10/site-packages/wntr: no
  network/controls.py: all

# user_request
"""
Pls, create a test that compares a unique run with h_max=24 and another one with 24 sucessive runs with h_max=1 and remembering the last tank levels. The idea is to check if the tank sequence of levels are the same. If this is the case, then the test is successful. With this info, we can create a version of the code that stores the tank levels of each hour and use it to speed up the simulation.
"""