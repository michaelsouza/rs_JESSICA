# system:
- Python 3.11
- EPANET 2.2
- WNTR 1.2.0 (python package)
- rich 13.9.2 (python package)

# sources:
paper: no
  main.md: all

networks: no
  any-town.inp: all
  any-town.md: none

codes: yes
  bbsolver.py: all  

.venv/lib/python3.10/site-packages/wntr: no
  network/controls.py: all

# user_request
Merge the tables for pressures and tank levels in show_node_values function. Add new columns to indicate the row context (node, tank, ..., pressure, level, ...)
"""
